# Agents

## Agent skills

When you need current framework or library documentation,
use the Context7 MCP tools.

Always check Context7 before making assumptions about
current Next.js, NestJS, React, Prisma, TypeScript,
shadcn/ui, Zod, and TanStack Query APIs.

### Issue tracker

Issues live as markdown files under `.scratch/` in this repo. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` at the repo root, ADRs in `docs/adr/`. See `docs/agents/domain.md`.
